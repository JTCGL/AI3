# Workflow Agent Instructions

These instructions extend ../AGENTS.md and the repository root AGENTS.md.

Workflow YAML should orchestrate repository commands, not duplicate build logic. Keep permissions minimal. The Linux GCC build is the initial required project check.
