# Platform Layer Instructions

- Own SDL and GLES resources here with deterministic teardown and checked initialization.
- Keep Termux selection in build configuration rather than runtime application branches.
